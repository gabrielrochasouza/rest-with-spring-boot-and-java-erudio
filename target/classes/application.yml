spring:
    datasource:
        url: jdbc:postgresql://localhost:5432/testdb?useTimezone=true&serverTimezone=UTC
        username: exati
        password: guiaflex
    jpa:
        hibernate:
            ddl-auto: update
        properties:
            hibernate:
                dialect: org.hibernate.dialect.PostgreSQLDialect
        show-sql: false
